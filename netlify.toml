[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite 7 requires Node 20.19+ or 22.12+. Pinned rather than left to Netlify's
  # default image, which does not guarantee either — the same version CI already
  # builds and tests against.
  NODE_VERSION = "22"
